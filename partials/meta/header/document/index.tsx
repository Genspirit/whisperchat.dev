import { Head } from "next/document";

export default function CustomHeader() {
  return (
    <Head>
      <meta
        name="description"
        content="A peer to peer chat ephemeral chat application"
      ></meta>
    </Head>
  );
}
