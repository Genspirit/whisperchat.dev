import { getAccessToken } from "~/services/traversal/twilio";

describe("Traversal/Twilio", () => {
  test("Retrieves access tokens", async () => {
    // const accessTokens = await getAccessToken();
    // expect(accessTokens).toHaveProperty("username");
    // expect(accessTokens).toHaveProperty("ice_servers");
    // expect(accessTokens["ice_servers"].length).toBeGreaterThan(0);
  });
});
